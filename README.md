# RP CABINET — TEST v104

## Что изменилось в v104

- Плашка программы использует постоянную ссылку на файл `RP-Helper.zip` из последнего GitHub Release.
- Название в панели изменено на `RP-Helper 1.0`.
- Добавлено короткое описание помощника для GTA5RP, Majestic RP и других RP-проектов с текущим модулем Bongo Cat.
- Панель получает версию из названия или тега последнего GitHub Release и автоматически обновляет её в заголовке и кнопке скачивания.
- Файл для скачивания выбирается по точному имени `RP-Helper.zip`.
- Результат проверки GitHub кэшируется на 5 минут; при недоступности API остаются версия `1.0` и рабочая ссылка на последний релиз.
- Для следующих версий менять код панели не нужно, если новый Release становится последним, а файл сохраняет имя `RP-Helper.zip`.

## Сохранено из v103

- Добавлен собственный favicon вместо стандартного серого глобуса браузера.
- Значок выполнен в стиле панели: тёмная скруглённая плашка с фиолетовыми буквами `RP`.
- Подключены SVG, PNG 32×32 и корневой `favicon.ico` для браузерных вкладок и закладок.
- Добавлена иконка 180×180 для закладок и ярлыков на телефоне.
- Добавлены иконки 192×192 и 512×512 вместе с `site.webmanifest`.
- Одинаковый значок используется в основной панели и публичных отчётах.

## Сохранено из v102

- Плашка рабочей программы в боковой панели больше не является заглушкой.
- Программа получила название `RP-Helper`.
- Нажатие по плашке сразу скачивает `RP-Helper.zip` из GitHub Release `program-v1.0.0`.
- Для загрузки используется постоянный релиз репозитория `wxzg4bgc6n-code/RP-CABINET-test`.
- Плашка доступна с компьютера и телефона и использует визуал текущей темы панели.

## Что загружать на GitHub

В корень репозитория загружается всё содержимое папки сборки с сохранением структуры:

- `index.html`
- `report.html`
- `favicon.ico`
- `site.webmanifest`
- `assets/`
- `css/`
- `data/`
- `js/`
- `documentation/`
- `_tools/`
- `README.md`
- `build-summary.json`

Папки `proof-service` в v104 нет.

Не загружать:

- `node_modules/`;
- `.env` и `.env.local`;
- OAuth client secret, refresh token и другие секреты;
- временные файлы и старые сборки.

## Обязательная настройка Google Cloud

Файлы v104 готовы, но Google не разрешит работу с Диском, пока API не включён в проекте.

1. Открыть Google Cloud Console и выбрать проект Firebase `rp-cabinet`.
2. Включить **Google Drive API**.
3. В OAuth consent screen → Data Access добавить scope:
   `https://www.googleapis.com/auth/drive.file`.
4. Убедиться, что домен GitHub Pages добавлен в Authorized domains и Authorized JavaScript origins текущего Web OAuth client.
5. В API key, указанном в `js/config/google-drive.js`, разрешить Google Drive API и ограничить использование доменом панели.
6. До публикации OAuth-приложения добавить нужные Google-аккаунты в список Test users.

Подробно: `documentation/GOOGLE_DRIVE_SETUP.md`.

## Где менять проект

| Изменение | Место |
|---|---|
| Каркас страницы и подключения | `index.html` |
| Иконка вкладки и закладок | `favicon.ico`, `site.webmanifest`, `assets/icons/` |
| Общие стили и темы | `css/core.css`, `css/features/` |
| Профиль, Google-вход и Firebase | `js/app.js` |
| Google Drive API, папки, загрузка и очистка | `js/services/google-drive-storage.js` |
| Настройки Drive | `js/config/google-drive.js` |
| Скриншоты и создание отчёта | `js/features/progress-proofs.js` |
| Аватар Google Drive | `js/features/google-drive-avatar.js`, `css/features/google-drive-avatar.css` |
| Публичный отчёт | `report.html`, `js/report.js`, `css/report.css` |
| Умный поиск | `js/features/smart-context-search.js`, `css/features/smart-context-search.css` |
| Мобильная оболочка и поиск | `js/features/mobile-navigation.js`, `css/responsive/mobile.css` |
| Нормализация и точечные Firestore-пути | `js/core/realtime-state.js` |
| Постоянные локальные ключи | `js/core/storage-config.js` |
| Версия панели | `js/core/version.js` |
| Ступени | `data/levels.js` |
| Задания повышения | `data/progress/` |
| Правила обязательных скриншотов | `data/progress/proof-rules.js` |
| Материалы повышения | `js/features/pinned-materials.js` |
| Сохранение раскрытых блоков | `js/features/surface-state.js` |
| Плашка, версия и ссылка скачивания RP-Helper | `js/features/sidebar-promo.js`, `css/features/sidebar-promo.css` |
| Основная информация USAF | `data/usaf/content.js` |
| Экзамены USAF | `data/usaf/tests.js` |
| Набор вопросов УАК/ПК | `data/usaf/law-data.js` |
| Экзамен УАК/ПК | `data/usaf/law-exam.js`, `css/features/tests.css` |
| Памятка УАК/ПК | `data/usaf/law-guide.js`, `css/features/usaf-law.css` |
| Архитектура | `documentation/ARCHITECTURE.md` |
| Автоматические проверки | `_tools/` |

## Синхронизация

После входа через Google единственным источником состояния профиля остаётся:

`users/{uid}/profiles/default_realtime`

Firebase хранит:

- профиль и карьерный контекст;
- галочки прогресса;
- закрепления и премию;
- `fileId`, `resourceKey`, размеры и названия скриншотов;
- идентификатор публичного отчёта;
- метаданные пользовательской аватарки.

Firebase не хранит изображения и Base64. Сами файлы принадлежат пользователю и находятся на его Google Диске.

Модель синхронизации v80 не изменена:

1. `onSnapshot` постоянно доставляет серверные изменения телефону и ПК.
2. Каждое действие обновляет только свой Firestore-путь.
3. Открытие или обновление страницы ничего не отправляет в облако.
4. Закрытое устройство сначала получает серверное состояние.
5. `localStorage` остаётся только кэшем.
6. Access token Google Drive не записывается в Firebase; локальный кэш хранит его только до времени окончания и только для соответствующего UID.

## Скриншоты и отчёты

- PNG, JPG и WEBP;
- до 15 МБ на файл;
- без лимита количества на пункт;
- каждый файл сразу получает закрытую от поиска публичную ссылку;
- публичная ссылка не раскрывает папку или остальные файлы пользователя;
- ручное удаление скриншота из прогресса сразу вызывает Google Drive `files.delete`;
- крестик доступен у каждого скриншота, а кнопка «Удалить все скриншоты» очищает текущий контекст одной операцией;
- удаление скриншота при активном отчёте также удаляет JSON отчёта и отключает публичную ссылку;
- отсутствующий на Диске файл можно убрать из панели без зависания;
- оставленные скриншоты и отчёты автоматически очищаются через 8 дней при следующем подключении Диска;
- удаление отчёта вручную окончательно удаляет его JSON и связанные скриншоты;
- старые ссылки v92 не считаются активными отчётами v104.

## Аватар

- одна активная пользовательская фотография;
- центральное квадратное кадрирование;
- WebP 512×512;
- загрузка открывается карандашом поверх аватара, а не из настроек профиля;
- на ПК карандаш появляется при наведении, на телефоне виден постоянно;
- перед сохранением доступны масштабирование и перемещение кадра;
- новая фотография сначала загружается и синхронизируется, затем старая удаляется;
- аватар не участвует в восьмидневной очистке;
- если пользовательский аватар ещё не загружен, используется фото Google, затем инициалы.
