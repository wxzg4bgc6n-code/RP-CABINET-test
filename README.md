# RP CABINET — TEST v92

## Что изменилось в v92

- На телефоне раскрытие «Где искать» переводит поиск в отдельный полноразмерный режим фильтра.
- Экранная клавиатура закрывается перед раскрытием фильтра и больше не сжимает окно.
- Пока фильтр открыт, результаты временно скрыты и не занимают его высоту.
- Список организаций и отделов занимает всё свободное место и прокручивается внутри окна.
- После сворачивания фильтра результаты автоматически возвращаются.
- Используется динамическая высота `100dvh` с совместимым резервным вариантом.
- Firebase, прогресс, скриншоты, отчёты и документ `default_realtime` не изменялись.

## Сохранено из v91

- «Где искать» является заметным отдельным блоком над результатами на ПК и телефоне.
- Фильтр получил контраст из текущей темы и состояния «Развернуть/Свернуть».
- Общие и мобильные стили поиска разделены между feature- и responsive-файлами.
- Мобильный CSS подключается после общих стилей и имеет правильный приоритет.

## Сохранено из v90

- Дефолтная область поиска вычисляется заново из актуального профиля при каждом открытии окна.
- Если в профиле выбран `ARMY → USAF`, поиск сразу ограничен USAF без ручного нажатия в «Где искать».
- Если выбран `ARMY → Academy`, дефолтная область автоматически переключается на Academy.
- Инициализация до восстановления Firebase больше не оставляет поиск с пустой или неправильной областью.
- Изменение отдела в уже открытой панели обновляет дефолтную область через наблюдение за полем профиля.
- Ручной расширенный выбор отделов сохраняется, пока пользователь сам не нажмёт «Использовать текущий отдел».
- На телефоне поиск встроен шестым пунктом непосредственно в нижнюю навигационную панель.
- Отдельная плавающая кнопка поиска, перекрывающая нижнюю панель, не используется.

## Что загружать на GitHub

В корень репозитория загружается всё содержимое папки сборки с сохранением структуры:

- `index.html`
- `report.html`
- `assets/`
- `css/`
- `data/`
- `js/`
- `documentation/`
- `proof-service/` без `node_modules`
- `_tools/`
- `README.md`
- `build-summary.json`

Не загружать:

- `node_modules/`;
- `.env` и `.env.local`;
- локальные секреты, токены и служебные ключи;
- временные файлы и старые сборки.

## Где менять проект

| Изменение | Место |
|---|---|
| Каркас страницы и подключения | `index.html` |
| Общие стили и темы | `css/core.css`, `css/features/` |
| Общая логика и вид поиска | `js/features/smart-context-search.js`, `css/features/smart-context-search.css` |
| Мобильная оболочка, нижняя навигация и мобильный вид поиска | `js/features/mobile-navigation.js`, `css/responsive/mobile.css` |
| Профиль, запуск, авторизация и Firebase | `js/app.js` |
| Нормализация состояния и точечные Firestore-пути | `js/core/realtime-state.js` |
| Постоянные локальные ключи | `js/core/storage-config.js` |
| Версия панели | `js/core/version.js` |
| Ступени | `data/levels.js` |
| Задания повышения | `data/progress/` |
| Правила обязательных скриншотов | `data/progress/proof-rules.js` |
| Интерфейс скриншотов и отчётов | `js/features/progress-proofs.js` |
| Готовая страница отчёта | `report.html`, `js/report.js`, `css/report.css` |
| Автоматические и ручные материалы повышения | `js/features/pinned-materials.js` |
| Сохранение раскрытых блоков | `js/features/surface-state.js` |
| Основная информация USAF | `data/usaf/content.js` |
| Экзамены USAF | `data/usaf/tests.js` |
| Памятка УАК/ПК | `data/usaf/law-guide.js`, `css/features/usaf-law.css` |
| Подробная архитектура | `documentation/ARCHITECTURE.md` |
| Карта общего и мобильного кода | `documentation/RESPONSIVE_STRUCTURE.md` |
| Краткие правила сборки | `documentation/QUICK_GUIDE.txt` |
| Автоматические проверки сборки | `_tools/` |
| Сервер загрузки скриншотов | `proof-service/` |

## Как добавлять новые повышения

1. Добавить название ступени в массив нужного отдела в `data/levels.js`.
2. Добавить задания с тем же названием ступени в `data/progress/<отдел>.js`.
3. Если для задания существует точный материал, добавить его сопоставление в `js/features/pinned-materials.js`.
4. Не связывать задание с общим экзаменом или памяткой только из-за похожих слов.
5. Задание без отдельного материала должно пропускаться.
6. Не переименовывать опубликованные задания без миграции: текст задания является ключом прогресса.
7. Правила скриншотов назначать в `data/progress/proof-rules.js`.

## Синхронизация

После входа через Google единственным источником актуального состояния является документ:

`users/{uid}/profiles/default_realtime`

Он хранит профиль, карьерный контекст, прогресс, выбранные ступени, закрепления, премию, метаданные скриншотов и ссылки отчётов.

Как работает синхронизация:

1. Телефон и ПК входят через один Google-аккаунт.
2. При запуске панель сначала читает серверный документ.
3. Постоянный `onSnapshot` доставляет подтверждённые изменения на все открытые устройства.
4. Пользовательское действие отправляет `update()` только для изменённого Firestore-пути.
5. Одна галочка не отправляет и не заменяет весь список прогресса.
6. Обновление страницы, авторизация и входящий снимок не вызывают обратную запись.
7. Закрытое устройство при открытии получает актуальный серверный снимок и не отправляет старый кэш назад.
8. `localStorage` используется только как последний отображённый кэш и резервная копия.
9. Офлайн-записи удерживает Firestore IndexedDB с `synchronizeTabs: true`; собственной очереди операций у панели нет.
10. Старые облачные документы читаются только для первоначальной миграции, если `default_realtime` ещё не создан.

Код синхронизации находится в `js/app.js` и `js/core/realtime-state.js`. Подробное описание — в `documentation/ARCHITECTURE.md`.

## Сервис скриншотов

Сервис Vercel используется для загрузки оригинальных изображений и формирования общей ссылки отчёта.

- Количество изображений на один пункт не ограничено.
- Максимальный размер одного файла — 15 МБ.
- Поддерживаются PNG, JPG и WEBP.
- Файлы автоматически удаляются через 8 дней.
- В Firebase хранятся только ссылки и метаданные, а не изображения или Base64.

Адрес сервиса: `https://rp-cabinet-proof-service.vercel.app`
