/*
 * Рабочий адрес сервиса отчётов в Vercel.
 * GitHub Pages остаётся адресом панели и страницы готового отчёта.
 */
window.RP_PROOF_SERVICE=Object.freeze({
  apiBase:'https://rp-cabinet-proof-service.vercel.app',
  maxFileBytes:15*1024*1024,
  maxFilesPerTask:10,
  reportLifetimeDays:8
});
