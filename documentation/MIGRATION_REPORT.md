# Переход v102 → v103

## Изменено в v103

- добавлены `favicon.ico`, SVG и PNG-варианты значка RP Cabinet;
- `index.html` и `report.html` используют одинаковую иконку вкладки;
- добавлена Apple Touch Icon для мобильных закладок;
- добавлен `site.webmanifest` с иконками 192×192 и 512×512;
- иконка не зависит от авторизации, профиля, Firebase или Google Drive.

## Сохранено из v102

- отключённая заглушка рабочей программы заменена активной ссылкой;
- название программы в панели изменено на `RP-Helper`;
- нажатие скачивает `RP-Helper.zip` напрямую из GitHub Release `program-v1.0.0`;
- ссылка на программу не затрагивает профиль, Firebase, Google Drive, прогресс и публичные отчёты.

## Сохранено из v101

- Drive access token перенесён из изолированного `sessionStorage` в общий между вкладками локальный кэш с обязательным `expiresAt`;
- сохранённый токен принимается только для текущего Firebase UID;
- старый сессионный токен автоматически переносится в новый кэш;
- вкладки панели получают обновление токена через событие `storage`;
- смена пользователя очищает кэш папок предыдущего Google Диска;
- повторная авторизация использует `login_hint` и не запрашивает выбор уже подключённого аккаунта.

## Сохранено из v100

- вопрос №7 УАК/ПК использует обычный список `answer-options` без отдельной сетки и собственных рамок;
- экзамен УАК/ПК расположен внутри блока «Экзамены других отделов»;
- удалены пояснение о будущих экзаменах и заглушка экзамена по вождению Supply Division.

## Сохранено из v99

- Экзамен УАК/ПК переведён на общие `qa-list`, `qa-card` и `answer-options`, как остальные тесты USAF.
- Удалены служебные строки о количестве вопросов и цветовом выделении ответов.
- Страницы формы 7–10 представлены одним вопросом №7 с четырьмя строками сопоставления; всего 17 карточек.
- Формулировки, варианты, порядок и повторы из присланных скриншотов сохранены.
- После сверки с материалами Downtown исправлены ответы по Миранде, сроку запроса звонка и адвоката, статьям 18.2 и 18.3.
- Памятка USAF приведена в соответствие с исправленным ключом.

## Сохранено из v98 и v97

- крестики удаления видны у всех скриншотов независимо от активного отчёта;
- удаление отдельного скриншота отключает связанный публичный отчёт;
- добавлено массовое удаление всех скриншотов текущего контекста;
- до четырёх файлов удаляются параллельно;
- уже отсутствующий на Диске файл больше не блокирует очистку записи в панели;
- при недоступном Drive пользователь может отдельно подтвердить очистку только локальных записей;
- неудачные массовые удаления сохраняются для повторной попытки.

## Сохранено из v96

- публичные карточки используют Drive thumbnail вместо полного оригинала;
- полноэкранный просмотр использует увеличенную миниатюру;
- media URL оставлен резервным источником;
- добавлены индикаторы загрузки изображений;
- аватар отчёта получил четыре последовательных источника;
- в новые манифесты записывается отдельное резервное Google-фото.

## Сохранено из v95

- аватар больше не пересоздаётся при каждом `render()`;
- добавлены локальный предпросмотр, кадрирование, перемещение и масштаб;
- добавлена клиентская оптимизация больших скриншотов;
- обновление процента загрузки отделено от общей перерисовки;
- скриншоты появляются по одному сразу после загрузки;
- прямые Drive-ссылки миниатюр заменены внутренним полноэкранным просмотром.

## Сохранено из v94

- удалён дублирующий запуск «Найти в материалах»;
- загрузка аватара перенесена из настроек на карандаш поверх аватара;
- подтверждено немедленное удаление скриншота через Google Drive `files.delete`;
- длинные значения `appProperties` заменяются стабильным отпечатком, чтобы Drive не отвечал 403;
- разбор ответа Drive показывает фактическое сообщение API.

## Удалено ранее в v93

- `proof-service/`;
- Vercel Functions;
- Vercel Blob Client;
- `js/vendor/vercel-blob-client.js`;
- `js/config/proof-service.js`;
- обращения к `/api/upload`, `/api/proofs/delete`, `/api/reports` и `/api/cleanup`;
- документация по переменным Vercel и Blob token.

## Добавлено ранее в v93

- `js/config/google-drive.js`;
- `js/services/google-drive-storage.js`;
- `js/features/google-drive-avatar.js`;
- `css/features/google-drive-avatar.css`;
- `documentation/GOOGLE_DRIVE_SETUP.md`.

## Формат файла доказательства

```json
{
  "provider": "google-drive",
  "fileId": "Drive file ID",
  "resourceKey": "optional resource key",
  "url": "public Drive API media URL",
  "name": "screen.webp",
  "type": "image/webp",
  "size": 123456,
  "width": 1920,
  "height": 1080,
  "uploadedAt": 0,
  "expiresAt": 0
}
```

## Формат отчёта

Отчёт является JSON-файлом версии 3 на Google Диске пользователя. `report.html` получает его напрямую через публичный Google Drive API key.

В Firebase сохраняются только:

- `provider`;
- `id` JSON-файла;
- `resourceKey`;
- публичная ссылка `report.html`;
- даты создания и окончания.

## Совместимость

- канонический профиль Firebase остаётся `default_realtime`;
- схема точечных обновлений остаётся v80;
- локальные ключи профиля не меняются;
- существующий прогресс, закрепления и премия не мигрируют в новый документ;
- старый активный отчёт v92 снимается из интерфейса, чтобы можно было сформировать новый Drive-отчёт;
- старые метаданные скриншотов могут быть показаны до замены, но новые загрузки идут только в Google Drive.

## Проверяемые сценарии

- вход через Google выдаёт Drive scope и сохраняет access token до `expiresAt` с привязкой к текущему Firebase UID;
- без Google загрузка отключена;
- загрузка нескольких изображений не удаляет предыдущие;
- удаление изображения вызывает Drive `files.delete`;
- отчёт открывается без Google-входа;
- отчёт не содержит перехода в панель;
- изображения увеличиваются внутри отчёта;
- замена аватара удаляет предыдущий файл только после успешной загрузки нового;
- входящий Firebase snapshot сохраняет `fileId`, `resourceKey` и аватар;
- обновление страницы не отправляет старый профиль обратно.
